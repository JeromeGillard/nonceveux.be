http:
  routers:
    phpmyadmin-route:
      rule: "Host(`phpmyadmin.nonceveux.be`)"
      tls:
        certresolver: myresolver
        domains:
          - main: "phpmyadmin.nonceveux.be"
          - sans: "*.nonceveux.be"
      entryPoints:
        - websecure
      service: phpmyadmin-srv
  
  services:
    phpmyadmin-srv:
      loadBalancer:
        servers:
          - url: "http://n_phpmyadmin:80"
